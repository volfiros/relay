import { EmptyState } from "../components/EmptyState";

export function LearningMode() {
  return (
    <section className="screen">
      <EmptyState title="Learning Mode" body="Resolved non-sensitive cases can be grouped by category for new moderator review." />
    </section>
  );
}
