import { EmptyState } from "../components/EmptyState";

export function AppealMemory() {
  return (
    <section className="screen">
      <EmptyState title="Appeal Memory" body="Resolved case memory will surface relevant retained cases by target, author, thread, or category." />
    </section>
  );
}
