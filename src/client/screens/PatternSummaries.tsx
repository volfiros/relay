import { EmptyState } from "../components/EmptyState";

export function PatternSummaries() {
  return (
    <section className="screen">
      <EmptyState title="Pattern Summaries" body="Relay summarizes repeated case metadata without recommending enforcement outcomes." />
    </section>
  );
}
